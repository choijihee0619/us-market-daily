pandas>=2.2
numpy>=1.26
pyarrow>=15.0
requests>=2.31
PyYAML>=6.0
python-dateutil>=2.9
pytz>=2024.1
matplotlib>=3.8
statsmodels>=0.14
scikit-learn>=1.4
scipy>=1.11
yfinance>=0.2.40
pandas-market-calendars>=4.4
feedparser>=6.0
lxml>=5.0

# 선택: 감성/임베딩 고도화 시
# transformers>=4.40
# torch>=2.2
# sentence-transformers>=3.0

# LLM 프로바이더. llm.provider 설정과 맞춰야 한다.
# 설치되어 있지 않으면 base.get_provider가 룰베이스로 폴백하므로 파이프라인은
# 죽지 않지만, 2번 블록 서술이 밋밋해진다. CI도 이 파일로 설치하므로
# provider를 openai로 두면 여기서 주석을 풀어야 한다.
openai>=1.40
# anthropic>=0.40

# 선택: 애널리틱스 (GA4 + AdSense). 없으면 CSV 폴백으로 동작
# google-analytics-data>=0.18
# google-api-python-client>=2.120
# google-auth>=2.29
